{
  "hash": "8f04f5fa8e8a945f9a39f96ba15ab975",
  "result": {
    "engine": "knitr",
    "markdown": "# Data Processing\n\n\n\nThe goal of this is a single dataframe that will be saved in a folder (I use `data\\saved data`) that can be imported at the beginning of the analysis scripts. More on that later.\n\nData processing has three main steps — merge the data, create the variables, and clean the data — that are done with code written in scripts. So, make a folder in scripts called **data processing** and open a new R script.\n\nI'm not going to assume you know anything about R, so I'll use this step to introduce some key ideas and concepts.\n\nOne of the big challenges in writing scripts is *readability*. You want to write something that a human can understand as easily as possible. Two little habits that go a very, very long way are sections and comments. Both rely on `#`, which tells the machine to ignore whatever follows on that line.\n\n::: {.callout-tip appearance=\"simple\" title=\"Coding Practices: Sections and Comments\"}\nSections are labeled blocks in scripts.\n\nCreate a section by starting a line with `#` and ending it with four or more dashes or equals signs.\n\n`# section example 1----`\n\n`#==== SECTION EXAMPLE 2 ====`\n\nYou can run all the code in a section (Code → Run Region → Run Code Section), fold/collapse a section, and jump to section headers using the pane between your script and the console.\n\nWithin sections, use (lots of) comments to explain the code in detail.\n\n`# create the women's share of applicants variable`\n:::\n\n## Import Raw Code\n\nLet's start by importing the data containing applications. The easiest way to do this is to navigate to your data in the Files pane of Rstudio and click on it. Rstudio will offer to import the data for you, but that's not reproducible. Instead, copy the code and paste it into your script.\n\n\n::: {.cell}\n\n```{.r .cell-code}\ndf <- read_csv(\"data/original data/IPEDS/ADM/adm2017_rv.csv\")\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError in `read_csv()`:\n! could not find function \"read_csv\"\n```\n\n\n:::\n:::\n\n\nGet used to errors. They happen a lot. Fortunately, you can paste error messages into a low-grade AI model and it can usually interpret it for you. This errors is saying it doesn't know the function `read_csv()`. That's because we didn't load the package first. Out of the box, R doesn't have a lot of functionality – this is intentional – but you can add functionality with packages which, very broadly, is a set of cohesive functions. The workhorse of data-wrangling is the tidyverse [@wickham2019tidyverse], a collection of R packages — including `readr`, `dplyr`, and `purrr` — built around a shared philosophy of tidy data. Load it once at the top of your script with `library(tidyverse)` and every function it provides, including `read_csv()`, becomes available.\n\nThis is an excellent place to add sections and comments.\n\n\n::: {.cell}\n\n```{.r .cell-code}\n#==== LOAD PACKAGES =====\n#auto installs:ggplot2, tibble, tidyr, readr, purrr, dplyr, stringr, forcats\nlibrary(tidyverse)\n\n#==== IMPORT ADM DATA ====\ndf <- read_csv(\"data/original data/IPEDS/ADM/adm2017_rv.csv\")\n```\n:::\n\n\nYou can have a look at the dataframe with `view(df)` or by clicking on it in the Environment pane in the upper right of Rstudio. It has 2075 rows and 68 columns. The sheer width of it makes it difficult to find the variables of interest. So, instead we just ask for the names of the variable in the console:\n\n\n::: {.cell}\n\n```{.r .cell-code}\nnames(df)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n [1] \"UNITID\"   \"ADMCON1\"  \"ADMCON2\"  \"ADMCON3\"  \"ADMCON4\"  \"ADMCON5\" \n [7] \"ADMCON6\"  \"ADMCON7\"  \"ADMCON8\"  \"ADMCON9\"  \"XAPPLCN\"  \"APPLCN\"  \n[13] \"XAPPLCNM\" \"APPLCNM\"  \"XAPPLCNW\" \"APPLCNW\"  \"XADMSSN\"  \"ADMSSN\"  \n[19] \"XADMSSNM\" \"ADMSSNM\"  \"XADMSSNW\" \"ADMSSNW\"  \"XENRLT\"   \"ENRLT\"   \n[25] \"XENRLM\"   \"ENRLM\"    \"XENRLW\"   \"ENRLW\"    \"XENRLFT\"  \"ENRLFT\"  \n[31] \"XENRLFTM\" \"ENRLFTM\"  \"XENRLFTW\" \"ENRLFTW\"  \"XENRLPT\"  \"ENRLPT\"  \n[37] \"XENRLPTM\" \"ENRLPTM\"  \"XENRLPTW\" \"ENRLPTW\"  \"XSATNUM\"  \"SATNUM\"  \n[43] \"XSATPCT\"  \"SATPCT\"   \"XACTNUM\"  \"ACTNUM\"   \"XACTPCT\"  \"ACTPCT\"  \n[49] \"XSATVR25\" \"SATVR25\"  \"XSATVR75\" \"SATVR75\"  \"XSATMT25\" \"SATMT25\" \n[55] \"XSATMT75\" \"SATMT75\"  \"XACTCM25\" \"ACTCM25\"  \"XACTCM75\" \"ACTCM75\" \n[61] \"XACTEN25\" \"ACTEN25\"  \"XACTEN75\" \"ACTEN75\"  \"XACTMT25\" \"ACTMT25\" \n[67] \"XACTMT75\" \"ACTMT75\" \n```\n\n\n:::\n:::\n\n\nIt might be easier to read the variables if they were in some order.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nsort(names(df))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n [1] \"ACTCM25\"  \"ACTCM75\"  \"ACTEN25\"  \"ACTEN75\"  \"ACTMT25\"  \"ACTMT75\" \n [7] \"ACTNUM\"   \"ACTPCT\"   \"ADMCON1\"  \"ADMCON2\"  \"ADMCON3\"  \"ADMCON4\" \n[13] \"ADMCON5\"  \"ADMCON6\"  \"ADMCON7\"  \"ADMCON8\"  \"ADMCON9\"  \"ADMSSN\"  \n[19] \"ADMSSNM\"  \"ADMSSNW\"  \"APPLCN\"   \"APPLCNM\"  \"APPLCNW\"  \"ENRLFT\"  \n[25] \"ENRLFTM\"  \"ENRLFTW\"  \"ENRLM\"    \"ENRLPT\"   \"ENRLPTM\"  \"ENRLPTW\" \n[31] \"ENRLT\"    \"ENRLW\"    \"SATMT25\"  \"SATMT75\"  \"SATNUM\"   \"SATPCT\"  \n[37] \"SATVR25\"  \"SATVR75\"  \"UNITID\"   \"XACTCM25\" \"XACTCM75\" \"XACTEN25\"\n[43] \"XACTEN75\" \"XACTMT25\" \"XACTMT75\" \"XACTNUM\"  \"XACTPCT\"  \"XADMSSN\" \n[49] \"XADMSSNM\" \"XADMSSNW\" \"XAPPLCN\"  \"XAPPLCNM\" \"XAPPLCNW\" \"XENRLFT\" \n[55] \"XENRLFTM\" \"XENRLFTW\" \"XENRLM\"   \"XENRLPT\"  \"XENRLPTM\" \"XENRLPTW\"\n[61] \"XENRLT\"   \"XENRLW\"   \"XSATMT25\" \"XSATMT75\" \"XSATNUM\"  \"XSATPCT\" \n[67] \"XSATVR25\" \"XSATVR75\"\n```\n\n\n:::\n:::\n\n\nOk! So, this dataframe contains what we need! It has\n\n- UNITID, which identifies the school\n\n- APPLCNM, the number of men applicants\n\n- APPLCNW, the number of women applicants\n\nBefore moving forward, notice the nesting in `sort(names(df))`. With more complicated code, nesting hampers readability so you'll want to use the pipe operator: `|>`. Then, instead of reading inside-out, you read left to right.\n\n::: {.callout-tip title=\"Coding Practices: Pipe Operator\"}\nThe pipe operator places the left-hand side into the *first* slot of the right-hand function.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nsort(names(df))\ndf |> names() |> sort()\n```\n:::\n\n\nThe shortcuts are:\n\n- **Windows/Linux:** `Ctrl` + `Shift` + `M`\n- **Mac:** `Cmd` + `Shift` + `M`\n:::\n\nReturning to the variables in the data, we have UNITID, APPLCNM, APPLCNW, but we don't have the year. We need to make a new variable that has the year. You can do that with the `mutate()` function from the *tidyverse* package.\n\n\n::: {.cell}\n\n```{.r .cell-code}\n#==== LOAD PACKAGES =====\n#auto installs:ggplot2, tibble, tidyr, readr, purrr, dplyr, stringr, forcats\nlibrary(tidyverse)\n\n#==== IMPORT ADM DATA ====\ndf <- read_csv(\"data/original data/IPEDS/ADM/adm2017_rv.csv\") |>\n  mutate(year = 2017)\n```\n:::\n\n\nNow, let's bring in the next file. We can copy and paste the code and just change the year.\n\n\n::: {.cell}\n\n```{.r .cell-code}\n#==== LOAD PACKAGES =====\n#auto installs:ggplot2, tibble, tidyr, readr, purrr, dplyr, stringr, forcats\nlibrary(tidyverse)\n\n#==== IMPORT ADM DATA ====\ndf <- read_csv(\"data/original data/IPEDS/ADM/adm2017_rv.csv\") |>\n  mutate(year = 2017)\ndf2 <- read_csv(\"data/original data/IPEDS/ADM/adm2018_rv.csv\") |>\n  mutate(year = 2018)\n```\n:::\n\n\nNow, we need to combine them. *If* the data have all the same columns – which they should in this particular instance – then you simply want to stack them (bind the rows together). Just for fun, I'm going to ask AI how to do this. I'll copy and paste my code into Google and ask it how to combine the data.\n\n![Asking an AI overview how to combine two years of data with `bind_rows()`](images/bind-rows.png){width=\"272\"}\n\nIt, correctly, suggests `bind_rows()`. Not to be too trusting, you quickly verify by typing in the console `?bind_rows()` which brings up the documentation. It seems legit so now we can consolidate. Now,\n\n\n::: {.cell}\n\n```{.r .cell-code}\n#==== LOAD PACKAGES =====\n#auto installs:ggplot2, tibble, tidyr, readr, purrr, dplyr, stringr, forcats\nlibrary(tidyverse)\n\n#==== IMPORT ADM DATA ====\ndf <- read_csv(\"data/original data/IPEDS/ADM/adm2017_rv.csv\") |>\n  mutate(year = 2017)\ndf2 <- read_csv(\"data/original data/IPEDS/ADM/adm2018_rv.csv\") |>\n  mutate(year = 2018)\n\n#combine dataframes\ndf <- df |> bind_rows(df2)\n```\n:::\n\n\nLet's do the next file. We copy and paste the df2 line, change the filename and the year, and then repeat binding in the last line.\n\n\n::: {.cell}\n\n```{.r .cell-code}\n#==== LOAD PACKAGES =====\n#auto installs:ggplot2, tibble, tidyr, readr, purrr, dplyr, stringr, forcats\nlibrary(tidyverse)\n\n#==== IMPORT ADM DATA ====\ndf <- read_csv(\"data/original data/IPEDS/ADM/adm2017_rv.csv\") |>\n  mutate(year = 2017)\ndf2 <- read_csv(\"data/original data/IPEDS/ADM/adm2018_rv.csv\") |>\n  mutate(year = 2018)\n\n#combine dataframes\ndf <- df |> bind_rows(df2)\n\n#overwrite df2 with next file (fine since that info is in df)\ndf2 <- read_csv(\"data/original data/IPEDS/ADM/adm2019_rv.csv\") |>\n  mutate(year = 2019) #correct filename and year\n\n#add to existing dataframe\ndf <- df |> bind_rows(df2)\n```\n:::\n\n\nWe could continue copying and pasting, but....\n\n::: {.callout-tip title=\"Coding Tip: Always be Automating\"}\nReplaced repeated processes with automation.\n:::\n\nNow that we understand our process, let's try to automate it. The process is\n\n- select a file in the folder\n\n- import the file\n\n- add the year\n\n- add it to the previously imported data\n\n- repeat for each file in the folder\n\nLet's automate the file selection. This is best done with the `here()` function from the *here* package which we really should have been using anyway.\n\nThis function constructs paths. It is very complementary to Projects, and you've already initialized one in your replication folder (see @sec-projects). `here()`, gives the file path to the location of the .Rproj file. You'll get a message about this when you load the package. Make sure it's not already loaded with `detach(\"package:here\", unload = TRUE)` -- it's ok if this throws an error. Then, load it with `library(here)`. You'll get a message in your console telling you where it \"starts\". Test this by typing here() into your console.\n\nWhen you add text as an argument to `here()` it adds it into the path so that `here(\"data\")` creates a path to the *data* folder, which should already exist. These paths can be stored as variables and used as arguments for the `here()` function.\n\n::: {.callout-tip title=\"Coding Practices: here()\"}\nConstruct file paths with the `here()` function instead of verbatim.\n\n\n::: {.cell}\n\n```{.r .cell-code}\n#don't do this\nfolder.path <- \"data/original data/IPEDS/ADM/adm2017_rv.csv\"\n\n#use here() instead\nfolder.path <- here(\"data\",\"original data\",\"IPEDS\",\"ADM\",\"adm2017_rv.csv\")\n\n#even better\nfolder.path <- here(\"data\",\"original data\",\"IPEDS\",\"ADM\")\nhere(folder.path,\"adm2017_rv.csv\")\n```\n:::\n\n:::\n\nWe can use this last bit to automate getting the file paths if we happen to have a list of the files in the directory.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nlibrary(here)\n\n#path to ADM folder\nfolder.path <- here(\"data\",\"original data\",\"IPEDS\",\"ADM\")\n\n#get list of files in ADM folder\nlf <- list.files(folder.path)\n\n#show list of files\nlf\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] \"adm2017_rv.csv\" \"adm2018_rv.csv\" \"adm2019_rv.csv\" \"adm2020_rv.csv\"\n[5] \"adm2021_rv.csv\" \"adm2022_rv.csv\" \"adm2023_RV.csv\" \"adm2024.csv\"   \n```\n\n\n:::\n:::\n\n\nWe've got the list and stored it as a value. You can access specific entries from the list by their numerical place. The first item on the list is `lf[1]` –\\> adm2017_rv.csv. The third, fourth, and fifth items are `lf[3:5]` –\\> adm2019_rv.csv, adm2020_rv.csv, adm2021_rv.csv\n\nThis means we can construct a file path using the list.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nlibrary(here)\nfolder.path <- here(\"data\",\"original data\",\"IPEDS\",\"ADM\")\nlf <- list.files(folder.path)\n\n#construct path to first file in folder\nhere(folder.path,lf[1])\n```\n:::\n\n\nSo, we can import the first file in the folder by simply using its numerical place in the list.\n\n\n::: {.cell}\n\n```{.r .cell-code}\n#==== LOAD PACKAGES =====\n#auto installs:ggplot2, tibble, tidyr, readr, purrr, dplyr, stringr, forcats\nlibrary(tidyverse)\nlibrary(here)\n\n#==== IMPORT ADM DATA ====\n#set ADM file path\nfolder.path <- here(\"data\",\"original data\",\"IPEDS\",\"ADM\")\n\n#get list of files in ADM folder\nlf <- list.files(folder.path)\n\n##read first file\n###index variable\nll <- 1\ndf <- read_csv(here(folder.path,lf[ll]))\n```\n:::\n\n\nNext, we need to automate adding the year variable. Fortunately, it's contained in the file name. We just need to extract the four-digit year sequence from the name – which is stored in R as a string (that's just what it's called). There are a handful of ways to do this (so AI will give you a few recommendations), but you're probably better off using the *tidyverse* method: `str_extract()`. Working with strings is a thing in and of itself and well beyond the scope of what we need to understand. So, it doesn't make a lot of sense (at first blush) why this work, but it does.\n\n\n::: {.cell}\n\n```{.r .cell-code}\n#extract four digit sequence\nlf[1] |> str_extract(\"\\\\d{4}+\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] \"2017\"\n```\n\n\n:::\n:::\n\n\nAdd this to our workflow.\n\n\n::: {.cell}\n\n```{.r .cell-code}\n#==== LOAD PACKAGES =====\n#auto installs:ggplot2, tibble, tidyr, readr, purrr, dplyr, stringr, forcats\nlibrary(tidyverse)\nlibrary(here)\n\n#==== IMPORT ADM DATA ====\n#set ADM file path\nfolder.path <- here(\"data\",\"original data\",\"IPEDS\",\"ADM\")\n\n#get list of files in ADM folder\nlf <- list.files(folder.path)\n\n##read first file\n###index variable\nll <- 1\ndf <- read_csv(here(folder.path,lf[ll])) |>\n  mutate(year = lf[ll] |> str_extract(\"\\\\d{4}+\"))\n```\n:::\n\n\nAgain, we could copy and paste this for each file in the folder and just move the index variable forward every time so the next lines would be\n\n\n::: {.cell}\n\n```{.r .cell-code}\n#==== LOAD PACKAGES =====\n#auto installs:ggplot2, tibble, tidyr, readr, purrr, dplyr, stringr, forcats\nlibrary(tidyverse)\nlibrary(here)\n\n#==== IMPORT ADM DATA ====\n#set ADM file path\nfolder.path <- here(\"data\",\"original data\",\"IPEDS\",\"ADM\")\n\n#get list of files in ADM folder\nlf <- list.files(folder.path)\n\n##read first file\n###index variable\nll <- 1\ndf <- read_csv(here(folder.path,lf[ll])) |>\n  mutate(year = lf[ll] |> str_extract(\"\\\\d{4}+\"))\n\n###index variable\nll <- 2\ndf2 <- read_csv(here(folder.path,lf[ll])) |>\n  mutate(year = lf[ll] |> str_extract(\"\\\\d{4}+\"))\n\ndf <- df |> bind_rows(df2)\n```\n:::\n\n\nWe could continue with the copy/paste, but let's try to Always be Automating, and use a `for` loop instead. It works like this\n\n- for each item in a sequence – (item in seq)\n\n- do stuff – {stuff}\n\nThe sequence that we want is the second, third, fourth, etc. item in the list of files. There are 8 items in the folder, but rather than hard-code the value, we can access the number of items by asking the length of the list: `length(lf)`.\n\n\n::: {.cell}\n\n```{.r .cell-code}\n#==== LOAD PACKAGES =====\n#auto installs:ggplot2, tibble, tidyr, readr, purrr, dplyr, stringr, forcats\nlibrary(tidyverse)\nlibrary(here)\n\n#==== IMPORT ADM DATA ====\n#set ADM file path\nfolder.path <- here(\"data\",\"original data\",\"IPEDS\",\"ADM\")\n\n#get list of files in ADM folder\nlf <- list.files(folder.path)\n\n##initialize dataframe\nll <- 1\ndf <- read_csv(here(folder.path,lf[ll])) |>\n  mutate(year = lf[ll] |> str_extract(\"\\\\d{4}+\"))\n\n##loop over remaining files\nfor (ll in 2:length(lf)){\n  #read file and add year variable\n  df2 <- read_csv(here(folder.path,lf[ll])) |>\n    mutate(year = lf[ll] |> str_extract(\"\\\\d{4}+\"))\n\n  #bind to exisiting dataframe\n  df <- df |> bind_rows(df2)\n}\n```\n:::\n\n\nThe model you'll estimate expects one long-format dataframe. *Long format* means one row per institution-year — the same institution appears on multiple rows, one for each year, rather than having separate columns like `share_2018`, `share_2019`, etc. This is what `feols()` and almost every other R modeling function expects.\n\nGetting there is three tasks: import the raw files and merge them into one dataframe, create the variables you actually need, and clean the result down to your final sample. Once you have that single clean dataframe, save it to `data/saved data/` — from then on, your analysis scripts can just load that one file instead of re-running the import every time, which makes it much easier to clear your environment and start fresh when you're working on the model itself.\n\n## Import Raw Data\n\nThe most explicit way to do this is to read each year's file separately and stack them with `bind_rows()` — you did something like this already for the ADM data. It works, but it's repetitive: the same five lines, once per year, with only the year changing.\n\nA loop removes the repetition. The tidyverse way to loop over files is `purrr::map_dfr()`, which reads each file, applies whatever you tell it to, and stacks the results into one dataframe automatically:\n\n``` r\nadm_files <- list.files(\"data/original data/IPEDS/ADM\", full.names = TRUE)\n\nadm <- map_dfr(adm_files, read_csv)\n```\n\nOnce each survey (ADM, HD, EF-C) is its own clean dataframe, join them together on `UNITID` — the institution ID IPEDS uses consistently across every survey — with `left_join()`. Use a left join starting from whichever dataframe defines your sample (ADM, since that's your outcome variable) so you don't accidentally drop institutions just because they're missing from a survey you joined in later.\n\n## Make Variables\n\nSplit this into three kinds of variables, and build them in that order:\n\n- **Outcome variables** — the female applicant share (`women_share`), and later, the out-of-state enrollment share for the extension.\n- **Policy variables** — which states banned abortion. This doesn't come from IPEDS; merge it in using `STABBR`, the state abbreviation.\n- **Control variables** — the original paper doesn't use any. Should you add any anyway? Worth thinking through before you decide either way — and worth explaining your reasoning in the write-up regardless of which way you land.\n\n## Clean Data\n\nRestrict your sample to match the paper as closely as you can: the top-100 ranked schools, 2018–2022. There isn't much more to do here beyond that filtering step and making sure your variable names are consistent going into the analysis.\n",
    "supporting": [
      "walker-data-processing_files"
    ],
    "filters": [
      "rmarkdown/pagebreak.lua"
    ],
    "includes": {},
    "engineDependencies": {},
    "preserve": {},
    "postProcess": true
  }
}